name: CommandsAsync
version: 1.0.0
main: dev.dauxanh.commandsasync.CommandsAsync
api-version: 1.13
authors: [DauXanh]

commands:
  commandsasync:
    aliases: [ca, casync]

permissions:
  ca.*:
    default: op
    children:
      ca.reload:
        default: false